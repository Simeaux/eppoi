using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Vuforia;
//using Vuforia.Barcode; // Aggiungi questa riga

public class QRReader : MonoBehaviour
{
    /*
        void Start()
        {
            // 1. Trova il componente BarcodeBehaviour sull'oggetto
            BarcodeBehaviour barcode = GetComponent<BarcodeBehaviour>();

            if (barcode != null)
            {
                // 2. Nella 11.4.4 l'evento corretto è questo:
                barcode.OnBarcodeDetected += OnQRDetected;
            }
            else
            {
                Debug.LogError("Manca il componente BarcodeBehaviour su questo oggetto!");
            }
        }

        // 3. Il metodo deve accettare il parametro BarcodeData
        private void OnQRDetected(BarcodeData data)
        {
            // data.Text contiene il contenuto del QR
            Debug.Log("QR Rilevato: " + data.Text);
        }
    */
}
